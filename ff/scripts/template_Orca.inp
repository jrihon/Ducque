! HF 6-31G* KeepDens NoKeepInts 

# KeepDens is very important because we require the .scfp file, which contains the densities
# Convergence of the SCF and geometry optimisation. We only do SCF calculations here though.
# Can also be in the header as keyword : VeryTightSCF
%scf
convergence VeryTight
end

*pdbfile 0 1 path/to/pdbfile
